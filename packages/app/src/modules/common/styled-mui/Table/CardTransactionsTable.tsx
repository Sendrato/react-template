import { Table } from '@mui/material';
import styled from 'styled-components';

export const CardTransactionsTable = styled(Table)`
  th {
    padding-left: 0.5rem !important;
    padding-right: 0.5rem !important;
  }
`;
